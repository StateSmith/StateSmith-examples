// Autogenerated with StateSmith
#include "MySm.h"
#include <stdbool.h> // required for `consume_event` flag
#include <string.h> // for memset

static void ROOT_enter(MySm* self);
static void ROOT_exit(MySm* self);

static void ANY_enter(MySm* self);
static void ANY_exit(MySm* self);
static void ANY_do(MySm* self);
static void ANY_ev1(MySm* self);

static void REGULAR_TRIGGERS_enter(MySm* self);
static void REGULAR_TRIGGERS_exit(MySm* self);
static void REGULAR_TRIGGERS_do(MySm* self);
static void REGULAR_TRIGGERS_ev1(MySm* self);

static void SHORTHAND_TRIGGERS_enter(MySm* self);
static void SHORTHAND_TRIGGERS_exit(MySm* self);
static void SHORTHAND_TRIGGERS_do(MySm* self);
static void SHORTHAND_TRIGGERS_ev1(MySm* self);

// This function is used when StateSmith doesn't know what the active leaf state is at compile time due to sub states
// or when multiple states need to be exited.
static void exit_up_to_state_handler(MySm* self, const MySm_Func desired_state_exit_handler);


void MySm_ctor(MySm* self)
{
    memset(self, 0, sizeof(*self));
}

static void exit_up_to_state_handler(MySm* self, const MySm_Func desired_state_exit_handler)
{
    while (self->current_state_exit_handler != desired_state_exit_handler)
    {
        self->current_state_exit_handler(self);
    }
}

void MySm_start(MySm* self)
{
    ROOT_enter(self);
    // ROOT behavior
    // uml: TransitionTo(ROOT.InitialState)
    {
        // Step 1: Exit states until we reach `ROOT` state (Least Common Ancestor for transition). Already at LCA, no exiting required.
        
        // Step 2: Transition action: ``.
        
        // Step 3: Enter/move towards transition target `ROOT.InitialState`.
        // ROOT.InitialState is a pseudo state and cannot have an `enter` trigger.
        
        // ROOT.InitialState behavior
        // uml: TransitionTo(REGULAR_TRIGGERS)
        {
            // Step 1: Exit states until we reach `ROOT` state (Least Common Ancestor for transition). Already at LCA, no exiting required.
            
            // Step 2: Transition action: ``.
            
            // Step 3: Enter/move towards transition target `REGULAR_TRIGGERS`.
            REGULAR_TRIGGERS_enter(self);
            
            // Step 4: complete transition. Ends event dispatch. No other behaviors are checked.
            self->state_id = MySm_StateId_REGULAR_TRIGGERS;
            // No ancestor handles event. Can skip nulling `self->ancestor_event_handler`.
            return;
        } // end of behavior for ROOT.InitialState
    } // end of behavior for ROOT
}

void MySm_dispatch_event(MySm* self, enum MySm_EventId event_id)
{
    MySm_Func behavior_func = self->current_event_handlers[event_id];
    
    while (behavior_func != NULL)
    {
        self->ancestor_event_handler = NULL;
        behavior_func(self);
        behavior_func = self->ancestor_event_handler;
    }
}

const char* MySm_state_id_to_string(const enum MySm_StateId id)
{
    switch (id)
    {
        case MySm_StateId_ROOT: return "ROOT";
        case MySm_StateId_ANY: return "ANY";
        case MySm_StateId_REGULAR_TRIGGERS: return "REGULAR_TRIGGERS";
        case MySm_StateId_SHORTHAND_TRIGGERS: return "SHORTHAND_TRIGGERS";
        default: return "?";
    }
}

////////////////////////////////////////////////////////////////////////////////
// event handlers for state ROOT
////////////////////////////////////////////////////////////////////////////////

static void ROOT_enter(MySm* self)
{
    // setup trigger/event handlers
    self->current_state_exit_handler = ROOT_exit;
}

static void ROOT_exit(MySm* self)
{
    // State machine root is a special case. It cannot be exited.
    (void)self;  // nothing to see here compiler. move along!
}


////////////////////////////////////////////////////////////////////////////////
// event handlers for state ANY
////////////////////////////////////////////////////////////////////////////////

static void ANY_enter(MySm* self)
{
    // setup trigger/event handlers
    self->current_state_exit_handler = ANY_exit;
    self->current_event_handlers[MySm_EventId_DO] = ANY_do;
    self->current_event_handlers[MySm_EventId_EV1] = ANY_ev1;
    
    // ANY behavior
    // uml: 1. enter, exit, do, ev1 / { print("enter,exit,do,ev1: $any_t"); }
    {
        // Step 1: execute action `print("enter,exit,do,ev1: $any_t");`
        print("enter,exit,do,ev1: $any_t");
    } // end of behavior for ANY
    
    // ANY behavior
    // uml: 2. enter, exit, do, ev1 / { print("manual ANY"); }
    {
        // Step 1: execute action `print("manual ANY");`
        print("manual ANY");
    } // end of behavior for ANY
}

static void ANY_exit(MySm* self)
{
    // ANY behavior
    // uml: 1. enter, exit, do, ev1 / { print("enter,exit,do,ev1: $any_t"); }
    {
        // Step 1: execute action `print("enter,exit,do,ev1: $any_t");`
        print("enter,exit,do,ev1: $any_t");
    } // end of behavior for ANY
    
    // ANY behavior
    // uml: 2. enter, exit, do, ev1 / { print("manual ANY"); }
    {
        // Step 1: execute action `print("manual ANY");`
        print("manual ANY");
    } // end of behavior for ANY
    
    // adjust function pointers for this state's exit
    self->current_state_exit_handler = ROOT_exit;
    self->current_event_handlers[MySm_EventId_DO] = NULL;  // no ancestor listens to this event
    self->current_event_handlers[MySm_EventId_EV1] = NULL;  // no ancestor listens to this event
}

static void ANY_do(MySm* self)
{
    // No ancestor state handles `do` event.
    
    // ANY behavior
    // uml: 1. enter, exit, do, ev1 / { print("enter,exit,do,ev1: $any_t"); }
    {
        // note: no ancestor consumes this event, but we output `bool consume_event` anyway because a user's design might rely on it.
        bool consume_event = false; // the `do` event is special in that it normally is not consumed.
        (void)consume_event; // avoid un-used variable compiler warning. StateSmith cannot (yet) detect if behavior action code sets `consume_event`.
        
        // Step 1: execute action `print("enter,exit,do,ev1: $any_t");`
        print("enter,exit,do,ev1: $any_t");
        
        // Step 2: determine if ancestor gets to handle event next.
        // No ancestor handles event. Ignore `consume_event` flag.
    } // end of behavior for ANY
    
    // ANY behavior
    // uml: 2. enter, exit, do, ev1 / { print("manual ANY"); }
    {
        // note: no ancestor consumes this event, but we output `bool consume_event` anyway because a user's design might rely on it.
        bool consume_event = false; // the `do` event is special in that it normally is not consumed.
        (void)consume_event; // avoid un-used variable compiler warning. StateSmith cannot (yet) detect if behavior action code sets `consume_event`.
        
        // Step 1: execute action `print("manual ANY");`
        print("manual ANY");
        
        // Step 2: determine if ancestor gets to handle event next.
        // No ancestor handles event. Ignore `consume_event` flag.
    } // end of behavior for ANY
    
    // ANY behavior
    // uml: 3. do, ev1 / { print("do,ev1: $any_e"); }
    {
        // note: no ancestor consumes this event, but we output `bool consume_event` anyway because a user's design might rely on it.
        bool consume_event = false; // the `do` event is special in that it normally is not consumed.
        (void)consume_event; // avoid un-used variable compiler warning. StateSmith cannot (yet) detect if behavior action code sets `consume_event`.
        
        // Step 1: execute action `print("do,ev1: $any_e");`
        print("do,ev1: $any_e");
        
        // Step 2: determine if ancestor gets to handle event next.
        // No ancestor handles event. Ignore `consume_event` flag.
    } // end of behavior for ANY
    
    // ANY behavior
    // uml: do, ev1 TransitionTo(REGULAR_TRIGGERS)
    {
        // Step 1: Exit states until we reach `ROOT` state (Least Common Ancestor for transition).
        ANY_exit(self);
        
        // Step 2: Transition action: ``.
        
        // Step 3: Enter/move towards transition target `REGULAR_TRIGGERS`.
        REGULAR_TRIGGERS_enter(self);
        
        // Step 4: complete transition. Ends event dispatch. No other behaviors are checked.
        self->state_id = MySm_StateId_REGULAR_TRIGGERS;
        // No ancestor handles event. Can skip nulling `self->ancestor_event_handler`.
        return;
    } // end of behavior for ANY
}

static void ANY_ev1(MySm* self)
{
    // No ancestor state handles `ev1` event.
    
    // ANY behavior
    // uml: 1. enter, exit, do, ev1 / { print("enter,exit,do,ev1: $any_t"); }
    {
        // note: no ancestor consumes this event, but we output `bool consume_event` anyway because a user's design might rely on it.
        bool consume_event = true; // events other than `do` are normally consumed by any event handler. Other event handlers in *this* state may still handle the event though.
        (void)consume_event; // avoid un-used variable compiler warning. StateSmith cannot (yet) detect if behavior action code sets `consume_event`.
        
        // Step 1: execute action `print("enter,exit,do,ev1: $any_t");`
        print("enter,exit,do,ev1: $any_t");
        
        // Step 2: determine if ancestor gets to handle event next.
        // No ancestor handles event. Ignore `consume_event` flag.
    } // end of behavior for ANY
    
    // ANY behavior
    // uml: 2. enter, exit, do, ev1 / { print("manual ANY"); }
    {
        // note: no ancestor consumes this event, but we output `bool consume_event` anyway because a user's design might rely on it.
        bool consume_event = true; // events other than `do` are normally consumed by any event handler. Other event handlers in *this* state may still handle the event though.
        (void)consume_event; // avoid un-used variable compiler warning. StateSmith cannot (yet) detect if behavior action code sets `consume_event`.
        
        // Step 1: execute action `print("manual ANY");`
        print("manual ANY");
        
        // Step 2: determine if ancestor gets to handle event next.
        // No ancestor handles event. Ignore `consume_event` flag.
    } // end of behavior for ANY
    
    // ANY behavior
    // uml: 3. do, ev1 / { print("do,ev1: $any_e"); }
    {
        // note: no ancestor consumes this event, but we output `bool consume_event` anyway because a user's design might rely on it.
        bool consume_event = true; // events other than `do` are normally consumed by any event handler. Other event handlers in *this* state may still handle the event though.
        (void)consume_event; // avoid un-used variable compiler warning. StateSmith cannot (yet) detect if behavior action code sets `consume_event`.
        
        // Step 1: execute action `print("do,ev1: $any_e");`
        print("do,ev1: $any_e");
        
        // Step 2: determine if ancestor gets to handle event next.
        // No ancestor handles event. Ignore `consume_event` flag.
    } // end of behavior for ANY
    
    // ANY behavior
    // uml: do, ev1 TransitionTo(REGULAR_TRIGGERS)
    {
        // Step 1: Exit states until we reach `ROOT` state (Least Common Ancestor for transition).
        ANY_exit(self);
        
        // Step 2: Transition action: ``.
        
        // Step 3: Enter/move towards transition target `REGULAR_TRIGGERS`.
        REGULAR_TRIGGERS_enter(self);
        
        // Step 4: complete transition. Ends event dispatch. No other behaviors are checked.
        self->state_id = MySm_StateId_REGULAR_TRIGGERS;
        // No ancestor handles event. Can skip nulling `self->ancestor_event_handler`.
        return;
    } // end of behavior for ANY
}


////////////////////////////////////////////////////////////////////////////////
// event handlers for state REGULAR_TRIGGERS
////////////////////////////////////////////////////////////////////////////////

static void REGULAR_TRIGGERS_enter(MySm* self)
{
    // setup trigger/event handlers
    self->current_state_exit_handler = REGULAR_TRIGGERS_exit;
    self->current_event_handlers[MySm_EventId_DO] = REGULAR_TRIGGERS_do;
    self->current_event_handlers[MySm_EventId_EV1] = REGULAR_TRIGGERS_ev1;
    
    // REGULAR_TRIGGERS behavior
    // uml: enter / { print("enter: REG"); }
    {
        // Step 1: execute action `print("enter: REG");`
        print("enter: REG");
    } // end of behavior for REGULAR_TRIGGERS
}

static void REGULAR_TRIGGERS_exit(MySm* self)
{
    // REGULAR_TRIGGERS behavior
    // uml: exit / { print("exit: REG"); }
    {
        // Step 1: execute action `print("exit: REG");`
        print("exit: REG");
    } // end of behavior for REGULAR_TRIGGERS
    
    // adjust function pointers for this state's exit
    self->current_state_exit_handler = ROOT_exit;
    self->current_event_handlers[MySm_EventId_DO] = NULL;  // no ancestor listens to this event
    self->current_event_handlers[MySm_EventId_EV1] = NULL;  // no ancestor listens to this event
}

static void REGULAR_TRIGGERS_do(MySm* self)
{
    // No ancestor state handles `do` event.
    
    // REGULAR_TRIGGERS behavior
    // uml: do / { print("do: REG"); }
    {
        // note: no ancestor consumes this event, but we output `bool consume_event` anyway because a user's design might rely on it.
        bool consume_event = false; // the `do` event is special in that it normally is not consumed.
        (void)consume_event; // avoid un-used variable compiler warning. StateSmith cannot (yet) detect if behavior action code sets `consume_event`.
        
        // Step 1: execute action `print("do: REG");`
        print("do: REG");
        
        // Step 2: determine if ancestor gets to handle event next.
        // No ancestor handles event. Ignore `consume_event` flag.
    } // end of behavior for REGULAR_TRIGGERS
    
    // REGULAR_TRIGGERS behavior
    // uml: do TransitionTo(SHORTHAND_TRIGGERS)
    {
        // Step 1: Exit states until we reach `ROOT` state (Least Common Ancestor for transition).
        REGULAR_TRIGGERS_exit(self);
        
        // Step 2: Transition action: ``.
        
        // Step 3: Enter/move towards transition target `SHORTHAND_TRIGGERS`.
        SHORTHAND_TRIGGERS_enter(self);
        
        // Step 4: complete transition. Ends event dispatch. No other behaviors are checked.
        self->state_id = MySm_StateId_SHORTHAND_TRIGGERS;
        // No ancestor handles event. Can skip nulling `self->ancestor_event_handler`.
        return;
    } // end of behavior for REGULAR_TRIGGERS
}

static void REGULAR_TRIGGERS_ev1(MySm* self)
{
    // No ancestor state handles `ev1` event.
    
    // REGULAR_TRIGGERS behavior
    // uml: ev1 / { print("ev1: REG"); }
    {
        // note: no ancestor consumes this event, but we output `bool consume_event` anyway because a user's design might rely on it.
        bool consume_event = true; // events other than `do` are normally consumed by any event handler. Other event handlers in *this* state may still handle the event though.
        (void)consume_event; // avoid un-used variable compiler warning. StateSmith cannot (yet) detect if behavior action code sets `consume_event`.
        
        // Step 1: execute action `print("ev1: REG");`
        print("ev1: REG");
        
        // Step 2: determine if ancestor gets to handle event next.
        // No ancestor handles event. Ignore `consume_event` flag.
    } // end of behavior for REGULAR_TRIGGERS
}


////////////////////////////////////////////////////////////////////////////////
// event handlers for state SHORTHAND_TRIGGERS
////////////////////////////////////////////////////////////////////////////////

static void SHORTHAND_TRIGGERS_enter(MySm* self)
{
    // setup trigger/event handlers
    self->current_state_exit_handler = SHORTHAND_TRIGGERS_exit;
    self->current_event_handlers[MySm_EventId_DO] = SHORTHAND_TRIGGERS_do;
    self->current_event_handlers[MySm_EventId_EV1] = SHORTHAND_TRIGGERS_ev1;
    
    // SHORTHAND_TRIGGERS behavior
    // uml: enter / { print("enter: SHORT"); }
    {
        // Step 1: execute action `print("enter: SHORT");`
        print("enter: SHORT");
    } // end of behavior for SHORTHAND_TRIGGERS
}

static void SHORTHAND_TRIGGERS_exit(MySm* self)
{
    // SHORTHAND_TRIGGERS behavior
    // uml: exit / { print("exit: SHORT"); }
    {
        // Step 1: execute action `print("exit: SHORT");`
        print("exit: SHORT");
    } // end of behavior for SHORTHAND_TRIGGERS
    
    // adjust function pointers for this state's exit
    self->current_state_exit_handler = ROOT_exit;
    self->current_event_handlers[MySm_EventId_DO] = NULL;  // no ancestor listens to this event
    self->current_event_handlers[MySm_EventId_EV1] = NULL;  // no ancestor listens to this event
}

static void SHORTHAND_TRIGGERS_do(MySm* self)
{
    // No ancestor state handles `do` event.
    
    // SHORTHAND_TRIGGERS behavior
    // uml: do / { print("do: SHORT"); }
    {
        // note: no ancestor consumes this event, but we output `bool consume_event` anyway because a user's design might rely on it.
        bool consume_event = false; // the `do` event is special in that it normally is not consumed.
        (void)consume_event; // avoid un-used variable compiler warning. StateSmith cannot (yet) detect if behavior action code sets `consume_event`.
        
        // Step 1: execute action `print("do: SHORT");`
        print("do: SHORT");
        
        // Step 2: determine if ancestor gets to handle event next.
        // No ancestor handles event. Ignore `consume_event` flag.
    } // end of behavior for SHORTHAND_TRIGGERS
    
    // SHORTHAND_TRIGGERS behavior
    // uml: ev1, do TransitionTo(ANY)
    {
        // Step 1: Exit states until we reach `ROOT` state (Least Common Ancestor for transition).
        SHORTHAND_TRIGGERS_exit(self);
        
        // Step 2: Transition action: ``.
        
        // Step 3: Enter/move towards transition target `ANY`.
        ANY_enter(self);
        
        // Step 4: complete transition. Ends event dispatch. No other behaviors are checked.
        self->state_id = MySm_StateId_ANY;
        // No ancestor handles event. Can skip nulling `self->ancestor_event_handler`.
        return;
    } // end of behavior for SHORTHAND_TRIGGERS
}

static void SHORTHAND_TRIGGERS_ev1(MySm* self)
{
    // No ancestor state handles `ev1` event.
    
    // SHORTHAND_TRIGGERS behavior
    // uml: ev1, do TransitionTo(ANY)
    {
        // Step 1: Exit states until we reach `ROOT` state (Least Common Ancestor for transition).
        SHORTHAND_TRIGGERS_exit(self);
        
        // Step 2: Transition action: ``.
        
        // Step 3: Enter/move towards transition target `ANY`.
        ANY_enter(self);
        
        // Step 4: complete transition. Ends event dispatch. No other behaviors are checked.
        self->state_id = MySm_StateId_ANY;
        // No ancestor handles event. Can skip nulling `self->ancestor_event_handler`.
        return;
    } // end of behavior for SHORTHAND_TRIGGERS
}


